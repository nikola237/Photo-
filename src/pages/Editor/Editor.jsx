import React from 'react';
import Navigation from '../../components/Navigation/Navigation';

const Editor = (props) => {
  console.log(props);
  return (
    <div>
      <Navigation />
      Editor!
    </div>
  );
};

export default Editor;
