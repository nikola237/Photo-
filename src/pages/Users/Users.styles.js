import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  table: {
    minWidth: 650,
  },
  itemContainer: {
    paddingLeft: 85,
    paddingRight: 50,
    paddingTop: 30,
  },
}));
